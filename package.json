{
  "name": "practice-hono-fastly",
  "type": "module",
  "scripts": {
    "dev": "fastly compute serve --watch",
    "prebuild": "tsc",
    "build": "js-compute-runtime build/index.js build/main.wasm",
    "deploy": "fastly compute publish"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@fastly/js-compute": "3.7.0",
    "hono": "^4.6.7"
  }
}
